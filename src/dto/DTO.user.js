export default class UserDTO {
    constructor({  fullname, email, phone, username, password, role }) {
        this.fullname = fullname
        this.email = email
        this.phone = phone
        this.username = username
        this.password = password
        this.role = role
    }
}

